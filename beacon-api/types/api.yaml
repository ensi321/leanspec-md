# Frontmatter
# ---
# last_synced_commit: 87943be
# source_files:
#   - src/lean_spec/spec/forks/lstar/containers.py
# related_prs: []
# ---

components:
  schemas:
    Checkpoint:
      type: object
      required: [root, slot]
      properties:
        root:
          $ref: './primitive.yaml#/components/schemas/Root'
        slot:
          $ref: './primitive.yaml#/components/schemas/Slot'
      description: |
        Snapshot of a specific block as a (root, slot) pair.
        See specs/lstar/beacon-chain.md#checkpoint.

    ErrorResponse:
      type: object
      required: [code, message]
      properties:
        code:
          type: integer
          description: HTTP status code mirror.
        message:
          type: string
          description: Human-readable error message.
        stacktraces:
          type: array
          items:
            type: string
          description: Optional debug stacktraces.
