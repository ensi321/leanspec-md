# Frontmatter
# ---
# last_synced_commit: 29a251c2
# source_files:
#   - src/lean_spec/node/api/handlers.py
#   - src/lean_spec/node/api/responses.py
#   - src/lean_spec/node/api/server.py
# related_prs: [1104]
# ---
#
# PR #1104 consolidated the per-resource handler files into ApiHandlers
# in handlers.py and the typed Pydantic response models in responses.py.
# The wire shape is unchanged: snake_case (root, slot) under CheckpointBody.

/lean/v0/checkpoints/justified:
  get:
    operationId: getCheckpointJustified
    summary: Get the latest justified checkpoint.
    description: |
      Returns the (root, slot) pair of the most recent justified checkpoint
      from the node's local store.

      Used by external monitors and tooling for tracking justification
      progress on the network.
    tags:
      - Beacon
    responses:
      '200':
        description: Latest justified checkpoint.
        content:
          application/json:
            schema:
              $ref: '../../../types/api.yaml#/components/schemas/Checkpoint'
